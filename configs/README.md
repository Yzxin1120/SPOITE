# Configurations

Configuration files will be frozen only after every `APPROVAL REQUIRED` item in
`../../experiment_design.md` has been resolved with the user.

No result-producing configuration exists yet.

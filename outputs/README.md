# Outputs

Output classes:

- `smoke/` - non-citable development checks;
- `provisional/` - complete runs awaiting validation;
- `final/` - approved tables and figures;
- `intermediate/` - resumable computational artifacts.
- `stale/` - superseded pre-freeze artifacts; never citable.

Every result must carry the provenance fields required by
`../../experiment_design.md`.
